<h1><%= title %></h1>

<%- messages() %>

<% if (errors) { %>
  <ul class="notice">
    <% errors.array().forEach(error => { %>
      <li><%= error.msg %></li>
    <% }) %>
  </ul>
<% } %>

<div id="inv-form-wrapper">
  <form action="/inv/update" method="post" id="editInventoryForm">
    <fieldset>
      <legend>Vehicle Details</legend>

      <input type="hidden" name="inv_id" value="<%= locals.inv_id %>" />

      <label for="classification_id">Classification <span class="required">*</span></label>
      <%- classificationList %>

      <label for="inv_make">Make <span class="required">*</span></label>
      <input
        type="text"
        name="inv_make"
        id="inv_make"
        value="<%= locals.inv_make %>"
        minlength="3"
        required
      />

      <label for="inv_model">Model <span class="required">*</span></label>
      <input
        type="text"
        name="inv_model"
        id="inv_model"
        value="<%= locals.inv_model %>"
        minlength="3"
        required
      />

      <label for="inv_year">Year <span class="required">*</span></label>
      <input
        type="number"
        name="inv_year"
        id="inv_year"
        value="<%= locals.inv_year %>"
        min="1900"
        max="2099"
        required
      />

      <label for="inv_description">Description <span class="required">*</span></label>
      <textarea
        name="inv_description"
        id="inv_description"
        required
      ><%= locals.inv_description %></textarea>

      <label for="inv_image">Image Path <span class="required">*</span></label>
      <input
        type="text"
        name="inv_image"
        id="inv_image"
        value="<%= locals.inv_image %>"
        required
      />

      <label for="inv_thumbnail">Thumbnail Path <span class="required">*</span></label>
      <input
        type="text"
        name="inv_thumbnail"
        id="inv_thumbnail"
        value="<%= locals.inv_thumbnail %>"
        required
      />

      <label for="inv_price">Price <span class="required">*</span></label>
      <input
        type="number"
        name="inv_price"
        id="inv_price"
        value="<%= locals.inv_price %>"
        min="0"
        step="0.01"
        required
      />

      <label for="inv_miles">Miles <span class="required">*</span></label>
      <input
        type="number"
        name="inv_miles"
        id="inv_miles"
        value="<%= locals.inv_miles %>"
        min="0"
        required
      />

      <label for="inv_color">Color <span class="required">*</span></label>
      <input
        type="text"
        name="inv_color"
        id="inv_color"
        value="<%= locals.inv_color %>"
        required
      />

      <button type="submit" class="inv-btn">Update Vehicle</button>
    </fieldset>
  </form>

  <p class="back-link"><a href="/inv/">&laquo; Back to Management</a></p>
</div>

<script src="/js/inv-update.js"></script>
